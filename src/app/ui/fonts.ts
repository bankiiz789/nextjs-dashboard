import { Inter, Kanit, Roboto } from "next/font/google";

export const kanit = Kanit({ weight: ["400"], subsets: ["latin-ext"] });
export const roboto = Roboto({ weight: ["700"], subsets: ["cyrillic"] });
